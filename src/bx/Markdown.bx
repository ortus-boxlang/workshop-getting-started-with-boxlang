class{

    function main( args ){
        while( true ){
            markdown = cliRead( "Markdown? " )
            println( markdown( markdown ) )
            sleep( 3000 )
            cliClear()
        }
    }

}